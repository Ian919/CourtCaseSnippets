import { styled } from '@mui/material/styles';
import { DialogContent } from '@mui/material';

export const CourtModalDialogContent = styled(DialogContent)({
  maxHeight: '70vh',
  overflowY: 'auto',
}); 